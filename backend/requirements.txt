# backend/requirements.txt

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Database
sqlalchemy==2.0.30
asyncpg==0.29.0
alembic==1.13.1

# Email
sendgrid==6.11.0

# Validation
pydantic[email]==2.7.1
pydantic-settings==2.3.1

# Environment
python-dotenv==1.0.1

# File handling
python-multipart==0.0.9
aiofiles==23.2.1
# Pin greenlet — newer versions have broken sqlalchemy 2.0.30 + asyncpg 0.29.0
# in a way that surfaces as the asyncpg+PgBouncer prepared-statement error,
# even with statement_cache_size=0.
greenlet==3.0.3

# Cloud storage
aioboto3==12.4.0

# Auth — verify Supabase JWTs. Supabase issues ES256-signed tokens via
# asymmetric signing keys; the [crypto] extra pulls in `cryptography`,
# which PyJWT needs for ECDSA verification.
pyjwt[crypto]==2.9.0
